node_modules/
dist/
coverage/
*.log

# Charles session captures — contain real tokens, emails and device identifiers.
# Never commit these; scrubbed fixtures live in test/fixtures/ instead.
.idea/
*.chlsj
*.chls

# Local probe credentials
.env
.env.local

# Local Homebridge instance for development — config.json holds the account
# password in plaintext, and persist/ holds the paired HomeKit keys.
.homebridge/

# Session and pinned-certificate cache written by scripts/diagnose.ts
.cache/

# Ad-hoc bundles and tarballs built by hand for trialling on a real Homebridge
# host. Never part of a release; the published artifact is dist/ alone.
dist-*/
*.tgz
