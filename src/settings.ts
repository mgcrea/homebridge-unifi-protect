/** Must match the `platform` key used in Homebridge's config.json. */
export const PLATFORM_NAME = "UniFiProtect";
/** Must match the package name exactly. */
export const PLUGIN_NAME = "@mgcrea/homebridge-unifi-protect";
export const MANUFACTURER = "Ubiquiti";
